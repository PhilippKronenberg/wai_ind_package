# Non-standard-evaluation column names and load()-created objects used in
# package code; declared to silence R CMD check "no visible binding" notes.
utils::globalVariables(c("mod", "name", "value"))
