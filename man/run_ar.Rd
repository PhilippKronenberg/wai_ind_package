% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backcast.R
\name{run_ar}
\alias{run_ar}
\title{Fit an AR(1) benchmark model and nowcast the target}
\usage{
run_ar(
  flows,
  stocks,
  target,
  date,
  dataset_used,
  stochastic_volatility = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{flows}{Named list of \code{ts} objects containing \code{target}.}

\item{stocks}{Named list of \code{ts} objects (unused, kept for a uniform
interface with \code{\link[=run_wai_adj]{run_wai_adj()}}).}

\item{target}{Character, name of the target series in \code{flows}.}

\item{date}{Numeric (decimal time), evaluation date used in the file
name when saving.}

\item{dataset_used}{Character, dataset label used as sub-directory
when saving.}

\item{stochastic_volatility}{Logical, unused; kept for a uniform
interface with \code{\link[=run_wai_adj]{run_wai_adj()}}.}

\item{output_dir}{Directory to save the fit to, or \code{NULL} (default) to
skip saving. When given, the fit is saved as
\code{file.path(output_dir, dataset_used, "fit_<date>.Rda")}.}
}
\value{
Invisibly, a list with elements \code{nowcast} and \code{nowcast_var}.
}
\description{
Estimates an AR(1) model on the target series and produces a one-step
nowcast with variance. Used as the benchmark model in the
out-of-sample evaluation.
}
