% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics-cor.R
\name{prepare_wai_qoq_series}
\alias{prepare_wai_qoq_series}
\title{Select the QoQ series for a WAI result, given the aggregation method}
\usage{
prepare_wai_qoq_series(wai_result, method)
}
\arguments{
\item{wai_result}{List with elements \code{tab_gr_qoq} and \code{tab_gr_lv} (as
produced by \code{\link[=extract_wai_data]{extract_wai_data()}}).}

\item{method}{Aggregation method; \code{"mean"} derives QoQ growth from the
level index, anything else returns the stored QoQ table.}
}
\value{
Data frame with \code{time} and \code{value}.
}
\description{
Select the QoQ series for a WAI result, given the aggregation method
}
\examples{
res <- list(tab_gr_qoq = data.frame(time = as.Date("2020-01-07"), value = 1),
            tab_gr_lv = data.frame(time = seq(as.Date("2020-01-07"), by = "week",
                                              length.out = 150),
                                   value = cumprod(1 + rnorm(150, 0, 0.002))))
prepare_wai_qoq_series(res, method = "last")
}
