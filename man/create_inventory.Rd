% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-helpers.R
\name{create_inventory}
\alias{create_inventory}
\title{Build an inventory of the model input series}
\usage{
create_inventory(flows, stocks)
}
\arguments{
\item{flows}{Named list of \code{ts} objects treated as flow variables.}

\item{stocks}{Named list of \code{ts} objects treated as stock variables.}
}
\value{
A data frame with one row per series and columns \code{key} (series
name), \code{type} (factor, \code{"flow"} or \code{"stock"}), \code{freq} (observations
per year), \code{mean} and \code{sd} (moments of the raw series, NA-removed).
}
\description{
Combines the flow and stock series lists into a data frame describing
each series: its name, type, frequency, and the mean and standard
deviation used for standardization in \code{\link[=prepare_data]{prepare_data()}}.
}
\examples{
data(data_ch_dataset_test)
inv <- create_inventory(flows = data_ch_dataset_test$flows,
                        stocks = data_ch_dataset_test$stocks)
head(inv)

}
