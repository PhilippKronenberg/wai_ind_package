% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-helpers.R
\name{prepare_data}
\alias{prepare_data}
\title{Standardize and align mixed-frequency series into one matrix}
\usage{
prepare_data(flows, stocks, inventory, target)
}
\arguments{
\item{flows}{Named list of \code{ts} objects treated as flow variables.}

\item{stocks}{Named list of \code{ts} objects treated as stock variables.}

\item{inventory}{Data frame from \code{\link[=create_inventory]{create_inventory()}}.}

\item{target}{Character, name of the target series (currently unused
here; kept for interface stability).}
}
\value{
A multivariate \code{ts} at the highest input frequency with one
column per series; missing values are encoded as \code{0}.
}
\description{
Standardizes each series using the moments from the inventory, aligns
all series on the highest-frequency time grid, and returns a single
multivariate \code{ts} matrix in which missing observations are encoded as
zero (as expected by the \code{\link[=hfdfm]{hfdfm()}} sampler).
}
\examples{
data(data_ch_dataset_test)
inv <- create_inventory(flows = data_ch_dataset_test$flows,
                        stocks = data_ch_dataset_test$stocks)
Ymat <- prepare_data(flows = data_ch_dataset_test$flows,
                     stocks = data_ch_dataset_test$stocks,
                     inventory = inv,
                     target = "ch.seco.gdp.real.gdp.ssa")
dim(Ymat)

}
