% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics-cor.R
\name{plot_comparison}
\alias{plot_comparison}
\title{Plot the WAI against a comparison indicator and GDP}
\usage{
plot_comparison(
  tab_wai,
  comparison_df,
  comparison_label,
  crises,
  hist_tab_gdp,
  sample_end_date,
  plot_title = NULL,
  ylim_fixed = NULL
)
}
\arguments{
\item{tab_wai}{WAI data frame (\code{time}, \code{value}).}

\item{comparison_df}{Comparison indicator data frame (\code{time}, \code{value}).}

\item{comparison_label}{Label for the comparison series.}

\item{crises}{Data frame with \code{Peak} and \code{Trough} dates for shading.}

\item{hist_tab_gdp}{GDP history in the legacy layout (\code{value} = Date,
\code{y} = GDP value).}

\item{sample_end_date}{Sample end (\code{Date}), e.g.
\code{wai_sample_config()$sample_end_date}.}

\item{plot_title}{Optional plot title.}

\item{ylim_fixed}{Optional fixed y-axis limits, length-2 numeric.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot the WAI against a comparison indicator and GDP
}
\examples{
wk <- seq(as.Date("2005-01-07"), by = "week", length.out = 900)
wai <- data.frame(time = wk, value = rnorm(900))
cmp <- data.frame(time = wk, value = rnorm(900))
crises <- data.frame(Peak = as.Date("2008-07-07"), Trough = as.Date("2009-09-28"))
gdp <- data.frame(value = seq(as.Date("2005-01-01"), by = "quarter", length.out = 60),
                  y = rnorm(60))
plot_comparison(wai, cmp, "Benchmark", crises, gdp,
                sample_end_date = as.Date("2021-12-31"))
}
