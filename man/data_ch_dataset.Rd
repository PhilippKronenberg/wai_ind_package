% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{data_ch_dataset}
\alias{data_ch_dataset}
\title{Harmonized Swiss indicator dataset for the WAI model}
\format{
A list with two components, as expected by \code{hfdfm()}:
\describe{
\item{flows}{Named list of 45 \code{ts} objects treated as flow
variables. The quarterly GDP target series is \emph{not} included;
the analysis scripts add it at runtime from the real-time GDP
vintage database that ships with the package at
\code{system.file("extdata", "realtime_database_GDP.xlsx", package = "waiind")}
(see \code{get_real_time_gdp_vintages()}).}
\item{stocks}{Named list of 7 \code{ts} objects treated as stock
variables.}
}
}
\source{
Produced from SECO, KOF, FSO, SNB, Datastream and further
high-frequency sources; see \code{data-raw/README_data_prep_dataset.md}
for the per-series preprocessing choices.
}
\usage{
data_ch_dataset
}
\description{
The curated, model-ready dataset produced by the data preparation
pipeline in \verb{data-raw/1_data_prep_dataset.R}. Mixed-frequency time
series are harmonized to the project conventions (weekly series use
48 observations per year) and transformed according to the variable
metadata in \code{data-raw/data_meta.xlsx}.
}
\keyword{datasets}
